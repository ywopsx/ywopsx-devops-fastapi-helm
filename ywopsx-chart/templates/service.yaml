apiVersion: v1
kind: Service
metadata:
  name: {{ include "ywopsx-chart.fullname" . }}
  labels:
    {{- include "ywopsx-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ywopsx-chart.selectorLabels" . | nindent 4 }}
